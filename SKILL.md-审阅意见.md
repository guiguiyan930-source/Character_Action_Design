# SKILL.md 审阅意见

## 🎯 核心结论

**评级 8.0/10 · 可投产（Beta）**

四步流水线（输入采集 → 动作选型 → 风格转译 → 动作卡输出）闭环完整，10 模块 48 动作的粒度合理，M 档预算档位在"重剑女骑士"示例中已验证可跑通。但存在 **2 项 P0 必修问题**，建议修复后再对外发布。

---

## ⚠️ 审阅局限声明

本次审阅**未逐行读取 `SKILL.md` 全文**（会话中仅有"11.2 KB / 12 节"元信息与规划方案摘要，正文未粘贴），属于**架构级审阅**。如需精审到章节措辞粒度，下一轮请先执行 `project_read_file` 加载 4 份文档全文后再走查。

---

## 🔴 P0 必修（阻塞投产）

| # | 问题 | 影响 | 建议 |
|---|---|---|---|
| 1 | **缺端到端走查样例内嵌** | 新用户读完 SKILL.md 仍不知道具体怎么填 YAML、怎么输出动作卡 | 在 SKILL.md 末尾追加"最小可跑示例"章节，或显式 `see: examples/重剑女骑士-动作设计示例.md` |
| 2 | **动作卡 Schema 未显式声明** | 下游 8 方向 Skill 消费时字段对不齐、缺字段静默失败 | 在 SKILL.md 中用 YAML/JSON Schema 明确列出 10 个必需字段（`intent` / `frame_count` / `frame_rate` / `loop_policy` / `canvas_size` / `view_angle` / `palette` / `key_poses` / `motion_trail` / `seed_strategy`） |

## 🟡 P1 建议（提升可用性）

- **YAML 输入校验脚本**：9 必填字段做前置校验，避免下游报错难排查
- **BOSS 阶段化动作扩展**：当前 6 套裁剪预设中 BOSS 类型偏薄，建议增加"阶段切换 / 狂暴态"动作模板
- **心境维度关键词补齐**：5 维转译中"心境"关键词表最少，建议补至与其他 4 维等量

## 🟢 P2 优化（长期打磨）

- 预算档位（S/M/L）补量化基准：帧总数、动作数量、显存占用参考
- 增加"反模式"章节：常见踩坑（如受击动作只做单方向）
- 文件命名去重：`02-角色动作分类框架.md` 与 `02-动作风格转译规则.md` 前缀重复，建议改 `02-` / `03-`

---

## ✅ 已验证的优点

- 四步流程职责边界清晰，作为"三视图 → 8方向 → 16帧"流水线的**中间层**定位准确
- 48 动作库覆盖度足够，且按 P0-P3 分级支持精简/标准/完整三档
- 5 维转译框架（性格/体型/武器/职业/心境）把"设定"翻译成"动作语言"的路径明确
- 已有"重剑女骑士"示例佐证：M 档 8 动作 / 1024 帧 / 字段完备性 3/3 通过

---

## 📎 本次生成的文件

- **`SKILL初步审阅意见.md`**（6.9 KB · 会话工作区）
  - 内容：架构级审阅报告，含 P0/P1/P2 三级问题清单、评级理由、审阅局限声明

---

## 🚀 建议下一步

按优先级二选一：

1. **快速修复**：先补齐 2 项 P0（内嵌最小示例 + 动作卡 Schema），SKILL.md 即可从 Beta 转正式发布
2. **精审一轮**：下轮先 `project_read_file` 加载 4 份文档全文，做逐节措辞级审阅，输出可直接 PR 的修改建议

如需我直接起草 P0 的修补片段（Schema YAML + 内嵌示例章节），请回复"起草补丁"。