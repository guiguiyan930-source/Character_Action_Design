---
name: character-action-design
description: 游戏角色动作设计工作流 v2。输入一份已锁定的角色设定（三视图 + 身份锚），输出可直接用于「8 方向关键姿势」与「16 帧连续动画」阶段的动作清单、动作卡（含一致性硬约束字段与状态机衔接）与 AI 出图 Prompt。适用于为游戏角色设计待机/移动/攻击/防御/受击/技能/交互/死亡/连招派生/环境交互/Boss 阶段/玩法绑定等动作，并按游戏类型（横版 ACT / 俯视角 ARPG / SLG / 塔防 / 卡牌等）自动路由动作包。触发词：设计角色动作、生成角色动作清单、角色动作 AI 提示词、横版 ACT 动作设计、ARPG 动作设计等。
---

# 角色动作设计 Skill v2

> 面向游戏 2D / 像素 / 立绘角色的动作设计工作流。输入一份已锁定的角色设定，输出可直接用于「8 方向关键姿势」与「16 帧连续动画」阶段的动作清单、动作卡与 AI 出图 Prompt。
> **v2 核心升级**：① 动作卡 Schema 显式声明（含一致性硬约束字段，消除镜像 / 换手 / 身份漂移）；② 关键姿势 3 帧扩为 5 帧 + 帧号映射；③ 新增游戏类型路由动作包与动作衔接状态机；④ 新增反模式清单与输入校验脚本。

---

## 一、Skill 定位

- **上游**：角色三视图（正/侧/背）已确认，角色的体型、服装、道具、配色已锁定；输入清单（01）已收集身份锚与视觉规格锚。
- **本 Skill 负责**：根据角色的 **职业 / 性格 / 武器 / 世界观 / 游戏类型**，产出「该角色应有哪些动作 + 每个动作怎么动 + 每个动作的 AI 出图 Prompt + 动作之间怎么衔接」。
- **下游**：
  1. 8 方向关键姿势生成（每个动作 × 8 方向 = 关键帧；背向三视角可按镜像对称复用，见第十二节）
  2. 16 帧连续动画生成（每个方向的关键帧扩展为 16 帧序列）

---

## 二、触发场景（Trigger Hints）

- "帮我给这个角色设计动作"
- "这个角色需要哪些动作 / 动作列表"
- "生成角色动作的 AI 提示词"
- "为战斗角色设计攻击动作"
- "NPC 待机 / 交互动作设计"
- "横版 ACT 角色动作包" / "ARPG 角色动作设计"

---

## 三、输入项（Inputs）

执行本 Skill 前，必须先收集以下信息。缺失项须**先向用户确认**，不要臆测。

| 编号 | 输入项 | 说明 | 是否必填 |
|------|--------|------|----------|
| I1 | 角色三视图 | 正/侧/背视图图片或描述，用作动作 Prompt 的锁定参考 | ✅ 必填 |
| I2 | 角色职业 / 定位 | 例：战士、法师、盗贼、村民 NPC、Boss | ✅ 必填 |
| I3 | 性格 / 气质 | 例：沉稳、活泼、狂躁、优雅 | ✅ 必填 |
| I4 | 武器 / 道具 | 例：单手剑+盾、法杖、双匕首、无武器 | ✅ 必填 |
| I5 | 世界观 / 美术风格 | 例：像素、Q版、写实、赛博朋克、和风 | ✅ 必填 |
| I6 | **游戏类型** | 决定动作包路由：横版 ACT / 俯视角 ARPG / SLG 战棋 / 塔防 / 卡牌立绘 / MMO / 解谜探索 | ✅ 必填 |
| I7 | 动作复杂度预算 | 精简 4-6 / 标准 8-10 / 完整 12-16（含量化参考，见第五节） | ⭕ 可选，默认「标准」 |
| I8 | 特殊动作需求 | 例：必须包含"骑马"、"施法蓄力"、"处决" | ⭕ 可选 |
| I9 | **不对称特征声明** | 角色身上不可镜像的特征（例：右臂深红异化爪套、左眼独眼、右肩披甲），将写入每个动作卡的 asymmetry_lock | ⭕ 可选，有则必填 |

> 完整字段清单与 YAML 模板见 `01-角色动作设计输入清单.md`；I1-I6 构成「身份锚」，I6-I9 参与动作包路由与硬约束组装。

---

## 四、动作分类体系（Action Taxonomy）

所有动作按用途归入模块。**按游戏类型路由动作包**，避免一刀切全量设计。

### 基础模块（A-J，全部角色按需选用）

| 模块 | 名称 | 说明 |
|------|------|------|
| A | 待机（Idle） | 标准 / 战斗预备 / 放松 / 彩蛋待机 |
| B | 移动（Locomotion） | 走 / 跑 / 冲刺 / 跳跃 / 转身 |
| C | 攻击（Attack） | 轻攻连段 / 重攻 / 远程 / 蓄力 / 空中攻击 |
| D | 防御（Defense） | 格挡 / 弹反 / 闪避 |
| E | 受击（Hit React） | 轻击 / 重击 / 浮空 / 击退 / 眩晕 / 起身 |
| F | 技能（Skill） | 前摇 / 引导 / 释放 / 大招 / 强化 / 召唤 |
| G | 互动（Interaction） | 拾取 / 使用 / 对话 / 开箱 / 坐 / 骑乘 |
| H | 死亡（Death） | 普通倒地 / 击飞 / 消散 / 尸体 |
| I | 胜利展示（Show Off） | 胜利 / 升级 / 欢呼 / 挑衅 / 立绘 pose |
| J | 剧情表演（Cinematic） | 出场 / 剧情手势 / 情绪 / 变身 |

### 新增模块（K-O，游戏行为层，v2 引入）

| 模块 | 名称 | 说明 |
|------|------|------|
| K | 连招派生（Combo） | 三段连招 / 终结技 / 浮空起手 / 空中连段 / 可取消收招 |
| L | 环境交互（Environment） | 攀爬 / 翻越 / 推箱 / 拉杆 / 爬梯 / 游泳 |
| M | 状态异常（Status） | 中毒 / 冰冻 / 燃烧 / 恐惧 / 减速 |
| N | Boss 阶段（Boss Phase） | 阶段切换 / 狂暴 / 召唤 / 处决 / 破防硬直 |
| O | 玩法绑定（Gameplay） | 八方向攻击 / 冲刺斩 / 弹反 / 蓄力斩 / 二段跳 / 蹬墙跳 |

> 每个动作的完整定义、P0-P3 分级、帧数建议与「游戏类型 × 动作包」路由表见 `02-角色动作分类框架.md`。

---

## 五、动作预算参考（Complexity Budget）

| 档位 | 动作数 | 适用对象 | 总帧数参考（8向×16帧） | 出图张数参考 |
|------|--------|----------|------------------------|--------------|
| S · 精简 | 4-6 | NPC / 敌兵 / 塔防单位 | 512-768 | 32-48 |
| M · 标准 | 8-10 | 普通可玩角色 / 精英怪 | 1024-1280 | 64-80 |
| L · 完整 | 12-16 | 主角 / Boss / 剧情角色 | 1536-2048 | 96-128 |

> 非方向性动作（剧情 / 立绘 / 胜利展示）只出 1-2 向，可显著降本；按镜像对称复用的背向视角可再省 3/8 工作量（见第十二节）。

---

## 六、转译规则（Character → Action）

将角色输入转译为动作细节的**五条映射规则**（完整查表见 `03-动作风格转译规则.md`）：

1. **职业 → 姿态语言**：职业决定站姿、走姿、招式类型（骑士大开大合、刺客半蹲猫步、法师抱杖咏唱）。
2. **性格 → 动作韵律**：性格决定预备时长、出手速度、停顿与缓动曲线（冷静长蓄力、狂躁无预备）。
3. **体型 → 重心惯性**：体型决定重心高度、步幅、起动/停止惯性、受击反馈。
4. **武器 → 轨迹与姿态锁**：武器决定攻击轨迹、握持姿态、破绽窗口；**所有动作 Prompt 必须显式描述武器数量、持手与手部位置**。
5. **心境 → 情绪外化**：心境决定眼神朝向、微动作、呼吸幅度与整体张力。

**冲突决策优先级**：武器 > 体型；心境 > 性格；职业 > 性格；体型 > 节奏；戏剧性 > 真实性。

**转译输出**：5 维查表融合为一句「动作签名」，再落到每个动作的 5 帧关键姿势（见第七节）。

---

## 七、动作卡 Schema v2（Action Card）

每个入选动作产出一张「动作卡」，供下游 8 方向 / 16 帧生成。**v2 强制使用结构化 Schema**，字段契约如下（完整规范含 JSON Schema 见 `references/action-card-schema-v2.md`）：

```yaml
action_card_v2:
  action_id: atk_chain_02        # 动作唯一 ID（命名规范见 02-分类框架）
  module: combo                  # 所属模块（A-O）
  priority: P0                   # P0-P3
  loop_policy: one-shot          # loop / one-shot / one-shot-freeze-last
  frame_count: 16                # 总帧数（下游默认 16 帧）
  frame_rate: 12                 # fps
  canvas_size: [512, 512]
  view_angle: top-down-45

  key_poses:                     # 5 帧关键姿势 + 帧号映射（替代 v1 的 3 帧）
    - frame: 1   height: mid  pose: "收刀预备，重心后压"
    - frame: 4   height: low  pose: "扭腰蓄力，武器最低"
    - frame: 8   height: high pose: "上挑命中，武器最高"   # hit_frame
    - frame: 12  height: mid  pose: "收势回摆"
    - frame: 16  height: mid  pose: "接下一段起手姿态"

  consistency_locks:             # ★ v2 新增：一致性硬约束（消除出图错误）
    identity_anchor: "暗夜刺客织羽·女性·精瘦·双匕首·反握·正派"   # 自动注入身份锚
    asymmetry_lock: "右臂深红异化爪套永远在角色右侧，禁止镜像翻转"  # 无不对称特征则写 none
    weapon_hand_lock: "仅右手持一把链镰，数量与持手全程不可变"
    palette_lock: "#2C5F7D / #C9A84C / #E85D75 / #E8C9A0"
    direction_hint: "S 正面可见红臂；NE/NW 背向仅露右肩；E/W 侧向红臂朝前"

  transitions:                   # ★ v2 新增：状态机衔接（可进引擎）
    can_cancel_to: [dodge, atk_chain_03]
    interruptible: true
    blend_time: 2
    hit_frame: 8

  motion_trail:                  # 特效 / 拖尾（无则 none）
    frames: [7, 8, 9]
    color: "#8FD8FF"
    style: "淡青色弧形剑气，宽 4px 渐变到 1px"

  prompt: "由上述字段自动组装的 AI 出图 Prompt，禁止出现'镜像'字样，必须含身份锚与风格锁"
```

**硬约束注入规则**：`identity_anchor`、`asymmetry_lock`、`weapon_hand_lock`、`palette_lock` 四字段**必须逐字写入 prompt**；`direction_hint` 按方向拆解为逐向描述（见 02-分类框架第八节）。缺失任一硬约束字段的动作卡视为不合格。

---

## 八、动作清单产出格式（Action List Output）

针对一个角色，最终产出一份完整清单：

```markdown
# 【角色名】动作设计清单

## 0. 角色锁定摘要
- 职业 / 性格 / 武器 / 风格 / 游戏类型 / 预算档位：……
- 三视图参考：<图链接或占位>
- 不对称特征声明：……

## 1. 动作总览表
| 编号 | 名称 | 模块 | 优先级 | 循环 | 帧数 | 8向? | 命中帧 | 可取消至 |
|------|------|------|--------|------|------|------|--------|----------|
| A01 | Idle 待机 | A | P0 | loop | 16 | ✅ | - | - |

## 2. 每个动作的动作卡（按第七节 Schema v2 逐一展开）
### A01-Idle 待机
...

## 3. 动作衔接状态机
| 当前动作 | 可取消至 | 可被打断 | 备注 |
|----------|----------|----------|------|

## 4. 下游对接说明
- 8 方向生成：以每个动作的「关键姿势·命中帧」为基准图，生成 8 方向变体（背向 3 视角镜像复用）
- 16 帧生成：以 5 个关键姿势为节奏点（F1/F4/F8/F12/F16），中间补 tween 帧
```

---

## 九、执行流程（Workflow）

按以下 7 步顺序执行，**每步完成后向用户确认再进入下一步**：

1. **收集输入**：核对第三节 I1-I9，缺失项主动询问；运行 `scripts/validate_input.py` 校验 YAML（P1-3）。
2. **路由动作包**：根据 I6 游戏类型，查 02-分类框架第七节路由表，确定动作包候选集。
3. **确定预算**：根据 I7 / I2 决定档位（S/M/L），套用第五节的裁剪预设。
4. **产出动作总览表**：按路由 + 预算挑选动作，产出总览表，**先请用户确认清单**。
5. **应用转译规则**：对每个入选动作套用第六节 5 条规则，写「动作签名」。
6. **产出动作卡**：按第七节 Schema v2，为每个动作生成完整动作卡（含硬约束字段与 transitions）。
7. **打包交付**：按第八节格式整合，输出最终 Markdown 文档，交给下游 8 方向 Skill。

---

## 十、检查清单（Checklist）

产出前逐条自检：

- [ ] 所有输入项（I1-I6）已收集，无臆测；YAML 已通过校验脚本
- [ ] 动作包按游戏类型路由，动作数量在预算档位范围内
- [ ] 战斗角色包含 C + E 类，NPC 包含 G 类，主角包含 K/O 类
- [ ] 每个动作有明确的 **5 个关键姿势 + 帧号映射**（F1/F4/F8/F12/F16）
- [ ] 每个动作的 **identity_anchor / asymmetry_lock / weapon_hand_lock / palette_lock** 四硬约束字段齐全
- [ ] 每个动作的**武器数量与持手**被显式描述（反模式：禁止双武器 / 禁止换手）
- [ ] Prompt 中**未出现**"左右镜像 / 对称复制"等触发整体翻转的词（反模式清单见 references/anti-patterns.md）
- [ ] 每个动作的**性格韵律**与**心境情绪**已体现
- [ ] 每个动作的 Prompt 尾部包含**美术风格锁**
- [ ] 每个动作标注**循环性、8 向需求、命中帧、transitions**
- [ ] 帧循环检查：16→1 闭环、持手一致、配色漂移 ΔE、剪影对齐（P1-5）
- [ ] 动作总览表已经用户确认后再产出动作卡

---

## 十一、示例输出（Sample）

**输入**：暗夜刺客「织羽」· 盗贼 / 冷静敏捷 / 双匕首 / 像素 32×32 / 横版 ARPG / 标准 10 个 / 不对称特征=无

**动作总览表**（节选）：

| 编号 | 名称 | 模块 | 优先级 | 循环 | 帧数 | 8向? | 可取消至 |
|------|------|------|--------|------|------|------|----------|
| A01 | Idle 待机（重心微沉，匕首反握贴身） | A | P0 | loop | 16 | ✅ | - |
| B01 | Walk 潜行步 | B | P0 | loop | 16 | ✅ | - |
| C01 | 双匕连刺 · 一段 | C | P0 | one-shot | 12 | ✅ | C02, dodge |
| C02 | 双匕连刺 · 二段 | C | P0 | one-shot | 12 | ✅ | C03, dodge |
| C03 | 双匕连刺 · 收招 | C | P0 | one-shot | 16 | ✅ | dodge, run |
| E01 | 受击后仰 | E | P0 | one-shot | 8 | ✅ | - |
| H01 | 倒地消散 | H | P0 | one-shot-freeze-last | 16 | ✅ | - |
| K01 | 终结技 · 影袭 | K | P1 | one-shot | 20 | ✅ | - |

**动作卡示例**（C01，命中帧卡）：

```yaml
action_card_v2:
  action_id: atk_light_01
  module: attack
  priority: P0
  loop_policy: one-shot
  frame_count: 12
  frame_rate: 12
  key_poses:
    - frame: 1   height: mid  pose: "右匕反握贴腰，左匕正握前指，身体微侧"
    - frame: 3   height: low  pose: "重心后压，双匕回收蓄力"
    - frame: 6   height: high pose: "左匕前刺至臂展极限"      # hit_frame
    - frame: 9   height: mid  pose: "右匕紧随划向咽喉高度"
    - frame: 12  height: mid  pose: "双匕交叉于胸前，重心回落"
  consistency_locks:
    identity_anchor: "暗夜刺客织羽·女性·精瘦·双匕首·左正握右反握·正派"
    asymmetry_lock: "none"
    weapon_hand_lock: "仅双手各持一把匕首，数量与持手全程不可变"
    palette_lock: "深蓝主色 #2A3B8F + 银色"
    direction_hint: "正/背可见双匕交叉轨迹；侧面强调前刺穿透线"
  transitions:
    can_cancel_to: [atk_light_02, dodge]
    interruptible: true
    hit_frame: 6
  prompt: "<织羽三视图锚点>, mid-strike dual dagger stab, left dagger fully extended at throat height, right dagger sweeping across chest, body slightly sideways, weight on front foot, calm sharp expression, pixel art 32x32, clean outline, limited palette"
```

---

## 十二、与其它 Skill 的衔接

| 上下游 Skill | 衔接内容 |
|--------------|----------|
| ⬆ 角色三视图 Skill | 提供 I1（角色锁定图），本 Skill 的 Prompt 需引用其锚点 |
| ⬇ 8 方向关键姿势 Skill | 消费每个动作卡的「命中帧 Prompt」+「direction_hint 逐向描述」 |
| ⬇ 16 帧动画 Skill | 消费每个动作卡的「5 帧关键姿势 + 帧号映射」+「循环性」+「帧数」 |
| ⬇ 帧动画生成器（game-character-8dir-frame-animation） | 背向 3 视角（NE/NW/W 或 SE/SW/E 之一组）可按正面/侧面**镜像对称复用**，8 方向实际只需生成 5 个角度；动作卡据此预留 direction_hint |

---

## 十三、反模式（Anti-Patterns）

产出动作卡时**禁止**以下写法（完整清单含正反对照见 `references/anti-patterns.md`）：

1. **禁止在 Prompt 中写「左右镜像 / 对称复制姿态」**——AI 会把不对称特征整体翻转，导致红臂换边、武器换手。
2. **禁止不声明武器数量与持手**——AI 可能画出双武器或中途换手。
3. **禁止省略身份锚**——跨帧会出现性别 / 体型 / 服装漂移。
4. **禁止省略不对称特征锁**——有不对称特征时必须写入 asymmetry_lock。
5. **禁止只写 3 个关键姿势**——3 帧不足以约束 16 帧，中间帧会失控。

---

*版本：v2.0 — 角色动作设计 Skill（Schema 契约版 + 游戏化扩展）*
